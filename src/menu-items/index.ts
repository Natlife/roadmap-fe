import dashboard from './dashboard';
import administration from './administration';
import type { MenuItems } from '@/types/menu';

const menuItems: MenuItems = {
  items: [dashboard, administration]
};

export default menuItems;
